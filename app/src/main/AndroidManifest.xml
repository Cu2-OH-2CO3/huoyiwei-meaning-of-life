<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.memoria.meaningoflife">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />
    <!-- 管理所有文件权限（Android 11+） -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <application
        android:name=".MeaningOfLifeApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MeaningOfLife">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 绘画模块 -->
        <activity android:name=".ui.painting.PaintingActivity" />
        <activity android:name=".ui.painting.WorkDetailActivity" />
        <activity android:name=".ui.painting.AddWorkActivity" />
        <activity android:name=".ui.painting.NodeEditActivity" />
        <activity android:name=".ui.painting.GoalManageActivity" />

        <!-- 日记模块 -->
        <activity android:name=".ui.diary.DiaryActivity" />
        <activity android:name=".ui.diary.DiaryWriteActivity" />
        <activity android:name=".ui.diary.DiaryDetailActivity" />
        <activity android:name=".ui.diary.DiaryStatisticsActivity" />

        <!-- 午餐模块 -->
        <activity android:name=".ui.lunch.LunchActivity" />
        <activity android:name=".ui.lunch.DishManageActivity" />
        <activity android:name=".ui.lunch.AddDishActivity" />

        <!-- 设置模块 -->
        <activity android:name=".ui.settings.PrivacyPolicyActivity" />
        <activity android:name=".ui.settings.IntroActivity" />
        <activity android:name=".ui.settings.LogViewerActivity" />
        <activity android:name=".ui.settings.DinoGameActivity" />
        <activity android:name=".ui.settings.BackgroundPreviewActivity" />

        <!-- 待办模块 - 新增 -->
        <activity
            android:name=".ui.task.TaskListActivity"
            android:parentActivityName=".ui.MainActivity"
            android:theme="@style/Theme.MeaningOfLife" />
        <activity
            android:name=".ui.task.TaskEditActivity"
            android:parentActivityName=".ui.task.TaskListActivity"
            android:theme="@style/Theme.MeaningOfLife" />

        <!-- 时间轴模块 -->
        <activity
            android:name=".ui.timeline.TimelineActivity"
            android:parentActivityName=".ui.MainActivity"
            android:theme="@style/Theme.MeaningOfLife" />
        <activity android:name=".ui.timeline.schedule.CourseEditActivity" />
        <activity android:name=".ui.timeline.schedule.SemesterManageActivity" />
        <activity android:name=".ui.timeline.schedule.IcsImportActivity" />
        <activity android:name=".ui.timeline.free.EventCreateActivity" />

        <!-- FileProvider（用于分享备份文件） -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
        </provider>
        <meta-data
            android:name="android.support.FILE_PROVIDER_PATHS"
            android:resource="@xml/file_paths" />




    </application>

</manifest>